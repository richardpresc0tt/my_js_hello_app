{
	"name": "my_js_hello_app",
	"version": "1.0.0",
  	"description": "",
	"main": "math.js",
	"scripts": {
		"start": "node math.js"
		"build": "echo 'Building project...' && mkdir -p dist && cp math.js dist/",
		"test": "jest"
	},
	"dependencies": {
		"express": "^4.18.s"
	}
	"devdependencies": {
		"jest": "^29.0.0"
	}
}